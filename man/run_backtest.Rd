% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_backtest}
\alias{run_backtest}
\title{Run backtest using a configuration list}
\usage{
run_backtest(config, df, ...)
}
\arguments{
\item{config}{A configuration list as returned by \code{default_backtest_config()}.}

\item{df}{Data frame with OHLC data (long format).}

\item{...}{Additional parameters to override the configuration.}
}
\value{
A list containing daily_positions, equity_curve, transactions, and config.
}
\description{
This is the main user-facing function. It accepts a configuration list
(typically created by \code{default_backtest_config()} and modified by
setter functions) and executes the backtest.
}
\examples{
\dontrun{
cfg <- default_backtest_config() \%>\%
  set_lot_size(200) \%>\%
  set_rebalance_cycle("monthly")
result <- run_backtest(cfg, my_data)
}
}
