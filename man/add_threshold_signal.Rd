% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_threshold_signal.R
\name{add_threshold_signal}
\alias{add_threshold_signal}
\title{Add Threshold Signal}
\usage{
add_threshold_signal(
  mkt_data,
  indicator_cols,
  threshold = 0,
  compare_op = ">",
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_cols}{Character vector of indicator column names.}

\item{threshold}{Numeric threshold(s). Length 1 (recycled) or same length as
\code{indicator_cols}.}

\item{compare_op}{Comparison operator(s): \code{">"}, \code{"<"}, \code{">="},
\code{"<="}, \code{"=="}, \code{"!="}. Length 1 (recycled) or same length as
\code{indicator_cols}.}

\item{signal_name}{Output column name. Auto-generated from first indicator and
operator if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Generates a 0/1 signal for each row where the indicator value satisfies a
comparison against a fixed threshold. Multiple indicator columns are combined
with AND logic (all conditions must hold).
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = 20)
result <- add_threshold_signal(df, indicator_cols = "mom_20",
                               threshold = 0, compare_op = ">")
}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
