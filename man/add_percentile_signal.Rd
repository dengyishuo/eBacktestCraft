% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_percentile_signal.R
\name{add_percentile_signal}
\alias{add_percentile_signal}
\title{Add Cross-Sectional Percentile Signal}
\usage{
add_percentile_signal(
  mkt_data,
  indicator_col,
  pct = 0.2,
  select = c("top", "bottom"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_col}{Character. Indicator column to rank cross-sectionally.}

\item{pct}{Numeric in (0, 1). Percentile threshold. Default \code{0.2} (top/bottom 20\%).}

\item{select}{Character. \code{"top"} (default) = highest values; \code{"bottom"} = lowest values.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Generates a signal for assets whose indicator value falls within the top or
bottom percentile of the cross-section on each date. Unlike
\code{\link{add_rank_signal}} which selects a fixed count, this selects a
fixed proportion of the pool.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = 20)
result <- add_percentile_signal(df, indicator_col = "mom_20",
                                pct = 0.3, select = "top")

}
\seealso{
Other signal-cross:
\code{\link[=add_quantile_signal]{add_quantile_signal()}},
\code{\link[=add_rank_signal]{add_rank_signal()}},
\code{\link[=add_score_signal]{add_score_signal()}},
\code{\link[=add_zscore_signal]{add_zscore_signal()}}
}
\concept{signal-cross}
