% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_opt_weight.R
\name{add_opt_weight}
\alias{add_opt_weight}
\title{Optimisation-based portfolio weights}
\usage{
add_opt_weight(
  mkt_data,
  signal_col,
  return_col,
  opt_type = "min_variance",
  window = 60L,
  alpha = 0.05,
  rf = 0,
  benchmark_col = NULL,
  annual_factor = 252L,
  weight_name = NULL,
  fallback = "equal",
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{Data frame with \code{date} and \code{code} columns.}

\item{signal_col}{Column where value = 1 marks the investable universe.}

\item{return_col}{Per-asset return column (already computed).}

\item{opt_type}{Objective. One of:
\code{"min_variance"}, \code{"risk_parity"},
\code{"max_sharpe"}, \code{"min_es"},
\code{"min_mdd"}, \code{"max_calmar"} / \code{"max_kama"},
\code{"max_treynor"} / \code{"max_terino"}.}

\item{window}{Rolling lookback periods. Default 60.}

\item{alpha}{Tail probability for ES (default 0.05).}

\item{rf}{Annual risk-free rate for Sharpe / Treynor. Default 0.}

\item{benchmark_col}{Benchmark return column required for \code{max_treynor}.}

\item{annual_factor}{Annualisation factor (252 / 52 / 12). Default 252.}

\item{weight_name}{Output column name. Auto-generated if NULL.}

\item{fallback}{Fallback when optimisation fails: \code{"equal"} or \code{"zero"}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended weight column.
}
\description{
For each trading date, fits portfolio weights by minimising / maximising
a risk-return objective computed over a rolling window of past returns.
}
\details{
Solvers used:
\itemize{
  \item \code{min_variance} — exact QP via \pkg{quadprog}.
  \item All others — unconstrained \code{stats::optim} with softmax
    reparameterisation (guarantees \eqn{w \ge 0}, \eqn{\sum w = 1}).
}
}
\seealso{
Other weight:
\code{\link[=add_inv_vol_weight]{add_inv_vol_weight()}},
\code{\link[=add_rank_weight]{add_rank_weight()}},
\code{\link[=add_target_vol_weight]{add_target_vol_weight()}},
\code{\link[=add_weight]{add_weight()}}
}
\concept{weight}
