% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ma_cross_signal.R
\name{add_ma_cross_signal}
\alias{add_ma_cross_signal}
\title{Add Moving Average Crossover Signal}
\usage{
add_ma_cross_signal(
  mkt_data,
  close_col = "close",
  fast_n = 5L,
  slow_n = 20L,
  fast_col = NULL,
  slow_col = NULL,
  mode = c("golden", "death", "both"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{close_col}{Character. Price column used to compute MAs. Default \code{"close"}.}

\item{fast_n}{Integer. Fast MA window. Default \code{5L}.}

\item{slow_n}{Integer. Slow MA window. Default \code{20L}.}

\item{fast_col}{Character. Pre-computed fast MA column name. If supplied,
\code{fast_n} and \code{close_col} are ignored for the fast leg.}

\item{slow_col}{Character. Pre-computed slow MA column name. If supplied,
\code{slow_n} is ignored for the slow leg.}

\item{mode}{Character. \code{"golden"} (default) = signal 1 on upward cross only;
\code{"death"} = signal 1 on downward cross only;
\code{"both"} = 1 on golden cross, -1 on death cross, 0 otherwise.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column.
}
\description{
Generates a signal when a fast moving average crosses above (golden cross,
signal = 1) or below (death cross, signal = -1) a slow moving average.
Both MAs are computed internally from a price column, or you can supply
pre-computed MA columns via \code{fast_col} / \code{slow_col}.
}
\examples{
data(style, package = "eBacktestCraft")
result <- add_ma_cross_signal(style, close_col = "adjusted",
                              fast_n = 5, slow_n = 20)

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
