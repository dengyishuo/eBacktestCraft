% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_breakout_signal.R
\name{add_breakout_signal}
\alias{add_breakout_signal}
\title{Add Breakout Signal}
\usage{
add_breakout_signal(
  mkt_data,
  close_col = "close",
  n = 20L,
  mode = c("up", "down", "both"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{close_col}{Character. Price column to test. Default \code{"close"}.}

\item{n}{Integer. Lookback window for rolling high/low. Default \code{20L}.}

\item{mode}{Character.
\code{"up"} (default) = signal 1 when price exceeds rolling high;
\code{"down"} = signal 1 when price falls below rolling low;
\code{"both"} = 1 on upward breakout, -1 on downward breakout.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column.
}
\description{
Generates a signal when the price breaks above the N-day high (upward breakout,
signal = 1) or below the N-day low (downward breakout, signal = 1 or -1).
Classic Donchian Channel / Turtle Trading trigger.
}
\examples{
data(style, package = "eBacktestCraft")
result <- add_breakout_signal(style, close_col = "adjusted", n = 20)

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
