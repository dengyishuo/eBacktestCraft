% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_crossover_signal.R
\name{add_crossover_signal}
\alias{add_crossover_signal}
\title{Add Crossover Signal}
\usage{
add_crossover_signal(
  mkt_data,
  indicator_col,
  cross_upper = NULL,
  cross_lower = NULL,
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_col}{Character. The column to watch for a crossover.}

\item{cross_upper}{Numeric or column name. Upper band — triggers a golden-cross
signal (1) when \code{indicator_col} crosses above it. Set \code{NULL} to
skip golden-cross detection.}

\item{cross_lower}{Numeric or column name. Lower band — triggers a death-cross
signal (1) when \code{indicator_col} crosses below it. Set \code{NULL} to
skip death-cross detection.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Detects when an indicator crosses above (golden cross) or below (death cross)
a band boundary. The band can be a fixed numeric value or another column.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_ma(style, close_col = "adjusted", n = c(5, 20))
result <- add_crossover_signal(df, indicator_col = "ma_5", cross_upper = "ma_20")

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
