% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_td_setup_signal.R
\name{add_td_setup_signal}
\alias{add_td_setup_signal}
\title{Add TD Sequential Setup Signal}
\usage{
add_td_setup_signal(
  mkt_data,
  setup_bull_col = "td_setup_bull",
  setup_bear_col = "td_setup_bear",
  mode = c("bull", "bear", "both"),
  window = 0L,
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and
\code{code}, already containing TD Setup columns.}

\item{setup_bull_col}{Character. Bullish setup count column name.
Default \code{"td_setup_bull"} (output of \code{add_td_setup()}).}

\item{setup_bear_col}{Character. Bearish setup count column name.
Default \code{"td_setup_bear"}.}

\item{mode}{Character.
\code{"bull"} (default) = signal 1 when bearish setup completes (= 9),
indicating bear exhaustion and potential long entry;
\code{"bear"} = signal 1 when bullish setup completes (= 9),
indicating bull exhaustion and potential short/exit entry;
\code{"both"} = 1 on bear exhaustion, -1 on bull exhaustion.}

\item{window}{Integer. If > 0, signal stays active for \code{window} bars
after the setup completes (lookback window). Default \code{0L} = signal
only on the exact bar where count = 9.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column.
}
\description{
Generates a signal when TD Sequential Setup completes (count = 9), based on
the output columns of \code{eTTR::add_td_setup()}. A bullish setup completion
(bear exhaustion) triggers a long signal; a bearish setup completion (bull
exhaustion) triggers a short signal.
}
\details{
This function requires that \code{eTTR::add_td_setup()} has already been
called on \code{mkt_data} to produce the setup count columns.
}
\examples{
\dontrun{
library(eTTR)
data(ettr_stocks)
df <- add_td_setup(ettr_stocks)
result <- add_td_setup_signal(df, mode = "bull", window = 5)
}

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
