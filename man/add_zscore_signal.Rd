% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_zscore_signal.R
\name{add_zscore_signal}
\alias{add_zscore_signal}
\title{Add Z-Score Signal}
\usage{
add_zscore_signal(
  mkt_data,
  indicator_col,
  threshold = 1,
  compare_op = ">",
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_col}{Character. Indicator column to standardise.}

\item{threshold}{Numeric. Z-score threshold. Default \code{1.0}.}

\item{compare_op}{Character. Comparison operator applied to the Z-score.
Default \code{">"} (select assets with Z-score above threshold).}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Computes the cross-sectional Z-score of an indicator on each date and
generates a signal for rows whose Z-score exceeds a threshold.
Useful for standardised factor selection where raw values are not comparable
across assets.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = 20)
result <- add_zscore_signal(df, indicator_col = "mom_20", threshold = 0.5)

}
\seealso{
Other signal-cross:
\code{\link[=add_percentile_signal]{add_percentile_signal()}},
\code{\link[=add_quantile_signal]{add_quantile_signal()}},
\code{\link[=add_rank_signal]{add_rank_signal()}},
\code{\link[=add_score_signal]{add_score_signal()}}
}
\concept{signal-cross}
