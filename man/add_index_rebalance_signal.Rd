% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_index_rebalance_signal.R
\name{add_index_rebalance_signal}
\alias{add_index_rebalance_signal}
\title{Add Index Rebalance Event Signal}
\usage{
add_index_rebalance_signal(
  mkt_data,
  rebalance_events,
  pre_window = 5L,
  post_window = 2L,
  direction_filter = c("both", "add", "remove"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{rebalance_events}{A data frame with columns:
\describe{
  \item{\code{code}}{Asset code.}
  \item{\code{rebalance_date}}{Date of index constituent change (Date or character).}
  \item{\code{direction}}{Character: \code{"add"} (inclusion) or \code{"remove"} (exclusion).}
}}

\item{pre_window}{Integer >= 0. Trading days before the rebalance date to
activate the signal. Default \code{5L}.}

\item{post_window}{Integer >= 0. Trading days after the rebalance date to
keep the signal active. Default \code{2L}.}

\item{direction_filter}{Character. \code{"add"} = only inclusion events;
\code{"remove"} = only exclusion events; \code{"both"} (default) = all events.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Generates a signal around index constituent change dates. Stocks being added
to an index tend to be bought up before the rebalance date (inclusion effect);
stocks being removed tend to be sold. This function activates a window around
each rebalance event date for the affected codes.
}
\examples{
\dontrun{
data(style, package = "eBacktestCraft")
events <- data.frame(
  code           = c("510300.SS", "513100.SS"),
  rebalance_date = as.Date(c("2022-06-10", "2022-12-09")),
  direction      = c("add", "add")
)
result <- add_index_rebalance_signal(style, rebalance_events = events,
                                     pre_window = 5, post_window = 2)
}

}
\seealso{
Other signal-event:
\code{\link[=add_earnings_signal]{add_earnings_signal()}},
\code{\link[=add_macro_signal]{add_macro_signal()}}
}
\concept{signal-event}
