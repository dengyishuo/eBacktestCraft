% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_rolling_signal.R
\name{add_rolling_signal}
\alias{add_rolling_signal}
\title{Generate signal based on rolling window statistics}
\usage{
add_rolling_signal(
  mkt_data,
  indicator_col,
  window = 20,
  n_sd = 2,
  direction = c("above", "below", "cross_above", "cross_below"),
  center_type = c("mean", "median"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{Data frame in long format, must contain 'date', 'code', and the
indicator column.}

\item{indicator_col}{Character string of column name to analyze (e.g., "close").}

\item{window}{Integer, rolling window length (e.g., 20 for 20-day).}

\item{n_sd}{Numeric, number of standard deviations for the band. Use 0 for
simple moving average only.}

\item{direction}{Character: "above" (signal when indicator > center + n_sd * sd),
"below" (indicator < center - n_sd * sd), or "cross_above" (crossover above
the upper band, requires previous day condition).}

\item{center_type}{Character: "mean" (default) or "median" for the central tendency.}

\item{signal_name}{Character, output signal column name. Auto-generated if NULL.}

\item{output}{Output format: "tibble" (default) or "data.frame"}
}
\value{
Original data frame with an appended signal column.
}
\description{
Create a 0/1 signal when the indicator value exceeds a rolling mean plus/minus
a multiple of rolling standard deviation (e.g., Bollinger Bands breakout) or
crosses a moving average. The rolling calculation is performed per asset.
}
\examples{
\dontrun{
# Example 1: Basic usage, Bollinger Band breakout signal
mkt_data <- data.frame(
  date   = rep(seq(as.Date("2023-01-01"), by = "day", length.out = 60), each = 3),
  code   = rep(c("AAPL", "MSFT", "GOOG"), times = 60),
  name   = rep(c("Apple", "Microsoft", "Alphabet"), times = 60),
  close  = round(runif(180, 100, 300), 2),
  open   = round(runif(180, 100, 300), 2),
  mom_20 = round(runif(180, -0.2, 0.2), 4),
  stringsAsFactors = FALSE
)
result <- add_rolling_signal(mkt_data,
  indicator_col = "close",
  window        = 20,
  n_sd          = 2,
  direction     = "above"
)

# Example 2: Key parameter variant, median-based crossover with 50-day window
result_cross <- add_rolling_signal(mkt_data,
  indicator_col = "close",
  window        = 50,
  n_sd          = 0,
  direction     = "cross_above",
  center_type   = "median",
  output        = "data.frame"
)

# Example 3: Backtest workflow, rolling signal -> equal weight -> run_backtest
mkt_data <- add_rolling_signal(mkt_data,
  indicator_col = "close", window = 20, n_sd = 2, direction = "above"
)
mkt_data <- add_equal_weight(mkt_data,
  signal_col = "signal_close_SMA20_sd2_above_upper"
)
# bt <- run_backtest(mkt_data, weight_col = "weight_equal_signal_close_SMA20_sd2_above_upper")
}
}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
