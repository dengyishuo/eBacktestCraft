% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_score_signal.R
\name{add_score_signal}
\alias{add_score_signal}
\title{Add Multi-Factor Score Signal}
\usage{
add_score_signal(
  mkt_data,
  indicator_cols,
  weights = NULL,
  top_n = 3L,
  normalize = TRUE,
  score_col = "composite_score",
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_cols}{Character vector. Indicator columns to combine.}

\item{weights}{Numeric vector. Weights for each indicator. Must be the same
length as \code{indicator_cols}. Need not sum to 1 (auto-normalised).
Default: equal weights.}

\item{top_n}{Integer. Number of top-scoring assets to select per date.
Default \code{3L}.}

\item{normalize}{Logical. If \code{TRUE} (default), cross-sectionally
Z-score each indicator before weighting to make scales comparable.}

\item{score_col}{Character. Name of the intermediate composite score column.
Default \code{"composite_score"}.}

\item{signal_name}{Output signal column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with two appended columns: \code{score_col} (numeric
  composite score) and the integer signal column (0/1).
}
\description{
Computes a composite score by taking a weighted sum of multiple indicator
columns (after optional cross-sectional Z-score normalisation), then selects
the top \code{top_n} assets by score on each date.
}
\details{
This is the standard multi-factor stock selection workflow:
normalise → weight → score → rank → select.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = c(10, 20))
result <- add_score_signal(df,
  indicator_cols = c("mom_10", "mom_20"),
  weights        = c(0.4, 0.6),
  top_n          = 3)

}
\seealso{
Other signal-cross:
\code{\link[=add_percentile_signal]{add_percentile_signal()}},
\code{\link[=add_quantile_signal]{add_quantile_signal()}},
\code{\link[=add_rank_signal]{add_rank_signal()}},
\code{\link[=add_zscore_signal]{add_zscore_signal()}}
}
\concept{signal-cross}
