% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_mean_reversion_signal.R
\name{add_mean_reversion_signal}
\alias{add_mean_reversion_signal}
\title{Add Mean Reversion Signal}
\usage{
add_mean_reversion_signal(
  mkt_data,
  indicator_col,
  n = 20L,
  k = 2,
  mode = c("long_only", "short_only", "both"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_col}{Character. Indicator column to test.}

\item{n}{Integer. Rolling window for mean and SD. Default \code{20L}.}

\item{k}{Numeric. Number of standard deviations that defines the band.
Default \code{2.0}.}

\item{mode}{Character.
\code{"long_only"} (default) = signal 1 only when below lower band;
\code{"short_only"} = signal 1 only when above upper band;
\code{"both"} = 1 below lower band, -1 above upper band.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column.
}
\description{
Generates a signal when the indicator deviates from its rolling mean by more
than \code{k} standard deviations. Useful for pairs trading, statistical
arbitrage, and overbought/oversold detection.
}
\details{
Signal = 1 when value is below mean - k*SD (oversold / long entry);
Signal = -1 when value is above mean + k*SD (overbought / short entry);
Signal = 0 within the band.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_rps(style, close_col = "adjusted", n = 60)
result <- add_mean_reversion_signal(df, indicator_col = "rps_60", n = 60, k = 1.5)

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
