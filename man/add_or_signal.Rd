% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_or_signal.R
\name{add_or_signal}
\alias{add_or_signal}
\title{Add OR-Combined Signal}
\usage{
add_or_signal(
  mkt_data,
  signal_cols,
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{signal_cols}{Character vector of existing 0/1 signal column names to combine.
Must be at least 2 columns.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Merges multiple existing 0/1 signal columns with OR logic: the output signal
is 1 when **any** input signal is 1. Use this to build composite opportunity
detectors where any single trigger is sufficient to enter.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = c(10, 20))
df <- add_threshold_signal(df, indicator_cols = "mom_10",
                           threshold = 0, compare_op = ">",
                           signal_name = "sig_mom10")
df <- add_threshold_signal(df, indicator_cols = "mom_20",
                           threshold = 0, compare_op = ">",
                           signal_name = "sig_mom20")
result <- add_or_signal(df, signal_cols = c("sig_mom10", "sig_mom20"))

}
\seealso{
Other signal-composite:
\code{\link[=add_and_signal]{add_and_signal()}},
\code{\link[=add_constant_signal]{add_constant_signal()}},
\code{\link[=add_multi_condition_signal]{add_multi_condition_signal()}},
\code{\link[=add_vote_signal]{add_vote_signal()}},
\code{\link[=add_window_signal]{add_window_signal()}}
}
\concept{signal-composite}
