% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_volatility_regime_signal.R
\name{add_volatility_regime_signal}
\alias{add_volatility_regime_signal}
\title{Add Volatility Regime Signal}
\usage{
add_volatility_regime_signal(
  mkt_data,
  close_col = "close",
  vol_n = 20L,
  method = c("threshold", "percentile"),
  vol_threshold = 0.2,
  hist_n = 252L,
  pct_threshold = 0.5,
  mode = c("low", "high"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{close_col}{Character. Price column used to compute returns and vol.
Default \code{"close"}.}

\item{vol_n}{Integer. Rolling window for realised volatility (annualised).
Default \code{20L}.}

\item{method}{Character.
\code{"threshold"} (default) = compare vol against a fixed \code{vol_threshold};
\code{"percentile"} = compare vol against its own rolling historical percentile
(lookback = \code{hist_n} days).}

\item{vol_threshold}{Numeric. Annualised volatility threshold used when
\code{method = "threshold"}. Default \code{0.20} (20\%).}

\item{hist_n}{Integer. Lookback window for historical percentile when
\code{method = "percentile"}. Default \code{252L}.}

\item{pct_threshold}{Numeric in (0,1). Percentile cutoff when
\code{method = "percentile"}. Default \code{0.5} (median).}

\item{mode}{Character. \code{"low"} (default) = signal 1 in low-vol regime;
\code{"high"} = signal 1 in high-vol regime.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Identifies high/low volatility regimes by comparing the rolling realised
volatility of each asset (or a reference index) against a threshold or its
own historical percentile. Analogous to a VIX-based risk-on/risk-off filter.
}
\details{
Signal = 1 in low-volatility regime (risk-on); 0 in high-volatility regime
(risk-off). Use \code{mode = "high"} to signal the high-vol regime instead.
}
\examples{
data(style, package = "eBacktestCraft")
result <- add_volatility_regime_signal(style, close_col = "adjusted",
                                       vol_n = 20, vol_threshold = 0.25)

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_regime_signal]{add_regime_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}}
}
\concept{signal-timeseries}
