% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_regime_signal.R
\name{add_regime_signal}
\alias{add_regime_signal}
\title{Add Market Regime Signal}
\usage{
add_regime_signal(
  mkt_data,
  close_col = "close",
  n = 200L,
  ma_col = NULL,
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{close_col}{Character. Price column used to determine regime.
Default \code{"close"}.}

\item{n}{Integer. Moving average window for regime identification.
Default \code{200L} (classic 200-day MA rule).}

\item{ma_col}{Character. Pre-computed MA column name. If supplied,
\code{n} and \code{close_col} are ignored.}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
  1 = bull regime (price above MA), 0 = bear regime.
}
\description{
Identifies bull/bear market regimes based on whether the price (or index) is
above or below a long-term moving average. Commonly used as a macro filter to
switch between risk-on and risk-off positioning.
}
\details{
Signal = 1 when in bull regime (price > MA); 0 when in bear regime.
}
\examples{
data(style, package = "eBacktestCraft")
result <- add_regime_signal(style, close_col = "adjusted", n = 120)

}
\seealso{
Other signal-timeseries:
\code{\link[=add_between_signal]{add_between_signal()}},
\code{\link[=add_breakout_signal]{add_breakout_signal()}},
\code{\link[=add_consecutive_signal]{add_consecutive_signal()}},
\code{\link[=add_crossover_signal]{add_crossover_signal()}},
\code{\link[=add_ma_cross_signal]{add_ma_cross_signal()}},
\code{\link[=add_mean_reversion_signal]{add_mean_reversion_signal()}},
\code{\link[=add_rolling_signal]{add_rolling_signal()}},
\code{\link[=add_td_setup_signal]{add_td_setup_signal()}},
\code{\link[=add_threshold_signal]{add_threshold_signal()}},
\code{\link[=add_volatility_regime_signal]{add_volatility_regime_signal()}}
}
\concept{signal-timeseries}
