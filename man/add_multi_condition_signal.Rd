% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_multi_condition_signal.R
\name{add_multi_condition_signal}
\alias{add_multi_condition_signal}
\title{Add Multi-Condition Signal}
\usage{
add_multi_condition_signal(
  mkt_data,
  indicator_cols,
  logic_op = "&",
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{indicator_cols}{Character vector of at least two indicator column names.}

\item{logic_op}{\code{"&"} (AND, default) or \code{"|"} (OR).}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Combines multiple indicator columns into a single signal using AND (\code{"&"})
or OR (\code{"|"}) logic. Each column is treated as truthy when its value is
greater than zero; \code{NA}/\code{Inf} values are treated as falsy.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = c(10, 20))
result <- add_multi_condition_signal(df,
  indicator_cols = c("mom_10", "mom_20"), logic_op = "&")

}
\seealso{
Other signal-composite:
\code{\link[=add_and_signal]{add_and_signal()}},
\code{\link[=add_constant_signal]{add_constant_signal()}},
\code{\link[=add_or_signal]{add_or_signal()}},
\code{\link[=add_vote_signal]{add_vote_signal()}},
\code{\link[=add_window_signal]{add_window_signal()}}
}
\concept{signal-composite}
