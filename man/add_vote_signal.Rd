% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_vote_signal.R
\name{add_vote_signal}
\alias{add_vote_signal}
\title{Add Voting Signal}
\usage{
add_vote_signal(
  mkt_data,
  signal_cols,
  min_votes = NULL,
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{signal_cols}{Character vector of existing 0/1 signal column names to vote on.
Must be at least 2 columns.}

\item{min_votes}{Integer. Minimum number of signals that must be active (= 1)
to trigger the output signal. Default \code{NULL} = strict majority
(\code{ceiling(length(signal_cols) / 2)}).}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Combines multiple existing 0/1 signal columns by majority vote: the output
signal is 1 when the number of active signals meets or exceeds
\code{min_votes}. More robust than AND (which is too strict) or OR (which
is too loose) when signals are noisy and partially correlated.
}
\examples{
data(style, package = "eBacktestCraft")
df <- eClassic::add_mom(style, close_col = "adjusted", n = c(5, 10, 20))
df <- add_threshold_signal(df, "mom_5",  threshold = 0, signal_name = "s1")
df <- add_threshold_signal(df, "mom_10", threshold = 0, signal_name = "s2")
df <- add_threshold_signal(df, "mom_20", threshold = 0, signal_name = "s3")
result <- add_vote_signal(df, signal_cols = c("s1", "s2", "s3"), min_votes = 2)

}
\seealso{
Other signal-composite:
\code{\link[=add_and_signal]{add_and_signal()}},
\code{\link[=add_constant_signal]{add_constant_signal()}},
\code{\link[=add_multi_condition_signal]{add_multi_condition_signal()}},
\code{\link[=add_or_signal]{add_or_signal()}},
\code{\link[=add_window_signal]{add_window_signal()}}
}
\concept{signal-composite}
