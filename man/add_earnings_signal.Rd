% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_earnings_signal.R
\name{add_earnings_signal}
\alias{add_earnings_signal}
\title{Add Earnings Event Signal}
\usage{
add_earnings_signal(
  mkt_data,
  earnings_dates,
  pre_window = 0L,
  post_window = 5L,
  mode = c("post", "pre", "both"),
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{earnings_dates}{A data frame with columns \code{code} and \code{earnings_date}
(Date or character coercible to Date). Each row is one earnings announcement.
Multiple announcements per code are supported.}

\item{pre_window}{Integer >= 0. Number of trading days before the earnings date
to activate the signal. Default \code{0L} (no pre-window).}

\item{post_window}{Integer >= 0. Number of trading days after the earnings date
to keep the signal active. Default \code{5L}.}

\item{mode}{Character.
\code{"pre"} = signal active only in the pre-window;
\code{"post"} (default) = signal active only in the post-window;
\code{"both"} = signal active in both windows (pre and post).}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column (0/1).
}
\description{
Generates a signal within a window around earnings announcement dates.
Useful for earnings drift strategies (PEAD), pre-announcement positioning,
or post-earnings momentum/reversal.
}
\examples{
\dontrun{
data(style, package = "eBacktestCraft")
earnings <- data.frame(
  code          = c("510300.SS", "510300.SS", "513100.SS"),
  earnings_date = as.Date(c("2021-04-30", "2021-10-31", "2021-04-28"))
)
result <- add_earnings_signal(style, earnings_dates = earnings,
                              pre_window = 2, post_window = 5)
}

}
\seealso{
Other signal-event:
\code{\link[=add_index_rebalance_signal]{add_index_rebalance_signal()}},
\code{\link[=add_macro_signal]{add_macro_signal()}}
}
\concept{signal-event}
