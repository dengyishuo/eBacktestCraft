% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_window_signal.R
\name{add_window_signal}
\alias{add_window_signal}
\title{Extend a Signal Over a Lookforward Window}
\usage{
add_window_signal(
  mkt_data,
  signal_col,
  window = 5L,
  carry_value = 1L,
  signal_name = NULL,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format data frame with columns \code{date} and \code{code}.}

\item{signal_col}{Character. Name of an existing 0/1 (or non-zero) signal column.}

\item{window}{Integer. Number of bars the signal stays active after the trigger
bar (inclusive). \code{window = 1} means only the trigger bar itself;
\code{window = 5} means the trigger bar plus the 4 following bars.}

\item{carry_value}{Numeric. Value written during the extended window.
Default \code{1L}. Use \code{-1} if the original signal encodes direction
and you want to preserve it (set \code{carry_value = NA} to copy the
original trigger value instead).}

\item{signal_name}{Output column name. Auto-generated if \code{NULL}.}

\item{output}{\code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
\code{mkt_data} with one appended integer signal column.
}
\description{
Given an existing 0/1 signal column, keeps the signal active for \code{window}
bars after each trigger. Useful when an event (e.g., TD Setup = 9, earnings
release) should influence positions for several days after it fires.
}
\examples{
data(style, package = "eBacktestCraft")
df <- add_breakout_signal(style, close_col = "adjusted", n = 20)
result <- add_window_signal(df, signal_col = "signal_breakout_bull", window = 5)

}
\seealso{
Other signal-composite:
\code{\link[=add_and_signal]{add_and_signal()}},
\code{\link[=add_constant_signal]{add_constant_signal()}},
\code{\link[=add_multi_condition_signal]{add_multi_condition_signal()}},
\code{\link[=add_or_signal]{add_or_signal()}},
\code{\link[=add_vote_signal]{add_vote_signal()}}
}
\concept{signal-composite}
